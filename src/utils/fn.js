  /* eslint-disable */
import request from "@/utils/request";

export const fn = {
    multipleAddProjectCodeCheck(options = {}, _self, callback) {
        let params = {
            ...options,
        };

        console.log("paramsx", params);
        return
    },
};